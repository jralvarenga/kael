---
"@kael/database": minor
"@kael/ui": patch
"web": patch
---

Update package versioning and database package dependencies.

- Bumped the `web` package from `0.0.0` to `0.0.1`.
- Added the new `@kael/database` package with dependencies including
  `dotenv`, `drizzle-orm`, and `pg`.
- Updated esbuild package versions to `0.25.12` for cross-platform
  compatibility.
