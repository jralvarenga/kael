---
"@kael/database": minor
---

Define the core database schema and update TypeScript configuration.

- Added database tables for `kael_table`, `kael_field`, `kael_row`, and
  `kael_record_link`.
- Updated `package.json` to export database types.
- Enhanced TypeScript configuration with ESNext module and bundler resolution.
